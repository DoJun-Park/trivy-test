name: 설정_개발환경 배포 (Script)
on:
  workflow_dispatch:
  push:
    branches:
      - test-serversss
  pull_request:
    branches:
      - test-serversss

env:
  EKS_VERSION: 1.32
  SERVICE_NAME: trivy-test
  DEPLOY_ENV: test
  BUILD_SERVICE_NAME: trivy-test
  NODE_VERSION: node-22
  DOCKER_PATH: ./Dockerfile


jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read          # Required to checkout and read repo files
      security-events: write  # Required to upload SARIF files to Security tab
      pull-requests: write    # Required to comment on pull requests

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js env
        run: |
          echo "NODE_HOME=/opt/${{ env.NODE_VERSION }}" >> $GITHUB_ENV
          echo "PATH=$PATH:/opt/${{ env.NODE_VERSION }}/bin" >> $GITHUB_ENV

      - name: Check Node version
        run: node --version

      - name: Set Short SHA
        id: slug
        run: echo "sha8=$(echo ${{ github.sha }} | cut -c1-8)" >> $GITHUB_OUTPUT
      
      - name: Set Image Tag
        env:
          IMAGE_TAG: ${{ steps.slug.outputs.sha8 }}
        run: |
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_ENV

      - name: Build Docker Image
        run: |
          docker build \
            -f ${{ env.DOCKER_PATH }} \
            -t trivy-test:${{ env.image_tag }} \
            -t trivy-test:latest \
            .
      
      - name: Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin v0.68.2
          trivy --version

      - name: Run Trivy vulnerability scanner (JSON)
        run: |
          trivy image \
            --format json \
            --output trivy-results.json \
            --severity CRITICAL,HIGH \
            --ignore-unfixed \
            trivy-test:${{ env.image_tag }}

      - name: Check vulnerabilities and fail if found
        id: check_vulns
        run: |
          VULN_COUNT=$(jq '[.Results[]?.Vulnerabilities[]?] | length' trivy-results.json)
          
          if [ "$VULN_COUNT" -gt 0 ]; then
            CRITICAL_COUNT=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length' trivy-results.json)
            HIGH_COUNT=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH")] | length' trivy-results.json)

            echo "has_vulns=true" >> $GITHUB_OUTPUT
            echo "::error title=취약점 발견::$VULN_COUNT개의 보안 취약점이 발견되었습니다 (CRITICAL: $CRITICAL_COUNT, HIGH: $HIGH_COUNT)"
            echo "❌ 보안 취약점 발견"
            echo "   - CRITICAL: $CRITICAL_COUNT개"
            echo "   - HIGH: $HIGH_COUNT개"
            echo ""
            echo "Security 탭에서 상세 결과 보기 : https://github.com/${{ github.repository }}/security/code-scanning?query=is%3Aopen+branch%3A${{ github.ref_name }}"
            exit 1
          else
            echo "has_vulns=false" >> $GITHUB_OUTPUT
            echo "✅ 취약점이 발견되지 않았습니다"
          fi

      - name: Run Trivy for SARIF output
        if: always() && steps.check_vulns.outputs.has_vulns == 'true'
        run: |
          trivy image \
            --format sarif \
            --output trivy-results.sarif \
            --severity CRITICAL,HIGH \
            --ignore-unfixed \
            trivy-test:${{ env.image_tag }}

      - name: Generate vulnerability summary
        if: always()
        run: |
          if [ -f trivy-results.json ]; then
            echo "## 🔍 Trivy 보안 취약점 스캔 결과" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            VULN_COUNT=$(jq '[.Results[]?.Vulnerabilities[]?] | length' trivy-results.json)
            
            if [ "$VULN_COUNT" -gt 0 ]; then
              echo "⚠️ **$VULN_COUNT개**의 취약점 발견 (CRITICAL, HIGH)" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "| 심각도 | CVE ID | 패키지 | 설치된 버전 | 수정 버전 | 제목 |" >> $GITHUB_STEP_SUMMARY
              echo "|--------|--------|--------|-------------|-----------|------|" >> $GITHUB_STEP_SUMMARY
              
              jq -r '.Results[]?.Vulnerabilities[]? | [.Severity, .VulnerabilityID, .PkgName, .InstalledVersion, (.FixedVersion // "N/A"), .Title] | @tsv' trivy-results.json | \
              while IFS=$'\t' read -r severity cve pkg installed fixed title; do
                cve_lower=$(echo "$cve" | tr '[:upper:]' '[:lower:]')
                echo "| $severity | [$cve](https://avd.aquasec.com/nvd/$cve_lower) | $pkg | $installed | $fixed | $title |" >> $GITHUB_STEP_SUMMARY
              done
              
              echo "" >> $GITHUB_STEP_SUMMARY
            else
              echo "✅ 취약점이 발견되지 않았습니다 (CRITICAL, HIGH)" >> $GITHUB_STEP_SUMMARY
            fi
          fi

      - name: Upload Trivy results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always() && steps.check_vulns.outputs.has_vulns == 'true'
        with:
          sarif_file: 'trivy-results.sarif'
